# 仓库协作规范

## 文档优先

每次修改代码前，必须先阅读以下文档：

1. `AGENTS.md`
2. `PROJECT_MAP.md`
3. `README.md`

如果文档描述与实际代码不一致，应在同一次修改中更新相关文档，确保文档反映最终实现。

## 项目定位

- 本仓库是 `go-sdk/core`、`go-sdk/database`、`go-sdk/server` 和 `go-sdk/taskkit` 的应用集成层。
- 包初始化只登记声明，不连接数据库、不执行迁移、不监听端口。
- `Run` 统一完成日志、数据库、迁移、业务初始化和服务生命周期。
- 本仓库提供约定和组合，不复制下层 SDK 已有能力。

## 修改原则

- 优先保持公开 API 简洁、行为统一和向后兼容。
- 修改前检查目录结构、调用关系和 Git 工作区状态，不覆盖或混入无关改动。
- 遵循现有包划分和代码风格，不引入缺少明确收益的依赖或抽象。
- 代码注释、维护文档和面向使用者的说明统一使用简体中文。
- 注释只描述最终设计意图、行为和关键约束，不记录修改历史，不复述代码。

## 设计约束

- 默认配置由 `core/config` 管理，配置键统一使用 `app.*`、`database.*`、`server.*` 和 `auth.*`。
- 数据库由本包读取 `database.driver` 和 `database.dsn` 后初始化，通过 `DB` 全局访问。
- 对外数据库类型统一使用 `dbx.DB`，本包不得直接暴露 `gorm.DB`。
- 具体数据库驱动由业务项目按需空导入，本包不得隐式导入全部驱动。
- `Register*` 只允许在 `Run` 前调用，适合由业务包的 `init` 完成声明式注册。
- 数据库迁移先于业务初始化执行，业务初始化先于 Server 启动执行。
- 普通 HTTP API 仍由 Proto 和 gRPC Gateway 定义；额外 HTTP 接口通过 `RegisterRoute` 注册。
- 不在日志、错误或文档中输出数据库 DSN、JWT 密钥及其他秘密。

## 验证边界

- 每次修改后必须使用项目已有的格式化工具，并检查 `git diff`、`git diff --check` 和工作区状态。
- 每次修改后必须依次运行 `make lint` 和 `make test`，完成条件是两条命令均无错误、无警告。
- 测试不得访问真实外部接口、数据库、消息队列、云资源或设备。
- 不执行部署、发布、上传或 `git push`；本地提交需要用户明确要求。

## 提交规范

- 提交信息遵循 Conventional Commits。
- 标题使用简洁英文；需要补充背景、影响或风险时写入提交正文。
- 只提交当前任务明确包含并已复核的文件。
